apiVersion: v1
kind: Secret
metadata:
  name: base-path
  annotations:
    homelab.dev/plaintext-reason: >-
      LAN storage coordinates only (RFC1918 address and NFS export paths),
      already public in README.md. No credential material.
type: Opaque
stringData:
  BASE_PATH: /tank
  NFS_SERVER: 192.168.50.10
  NFS_ROOT: /tank/k3s-pvcs
