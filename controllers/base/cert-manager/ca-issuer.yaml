---
# CA issuer. The CA keypair (homelab-ca-tls) is provided by ESO from the
# secret backend (see ca-externalsecret.yaml).
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: ca-issuer
spec:
  ca:
    secretName: homelab-ca-tls
