apiVersion: v1
kind: PersistentVolume
metadata:
  name: notifiarr-config-nfs
spec:
  capacity:
    storage: 5Gi
  accessModes: ["ReadWriteMany"]
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  mountOptions:
    - nfsvers=4.1
    - hard
    - noatime
  nfs:
    server: ${NFS_SERVER}
    path: ${NFS_ROOT}/notifiarr
  claimRef:
    namespace: media
    name: notifiarr-config-nfs
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: notifiarr-config-nfs
spec:
  accessModes: ["ReadWriteMany"]
  storageClassName: ""
  volumeName: notifiarr-config-nfs
  resources:
    requests:
      storage: 5Gi
