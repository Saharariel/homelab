apiVersion: v1
kind: PersistentVolume
metadata:
  name: qbittorrent-config-nfs
spec:
  capacity:
    storage: 5Gi
  accessModes: ["ReadWriteMany"]
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  mountOptions:
    - nfsvers=4.1
    - hard
    - noatime
  nfs:
    server: ${NFS_SERVER}
    path: ${NFS_ROOT}/qbittorrent
  claimRef:
    namespace: media
    name: qbittorrent-config-nfs
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: qbittorrent-config-nfs
spec:
  accessModes: ["ReadWriteMany"]
  storageClassName: ""
  volumeName: qbittorrent-config-nfs
  resources:
    requests:
      storage: 5Gi
