apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - helmrelease.yaml
  - helmrepo.yaml
  - externalsecret.yaml
  - discord-externalsecret.yaml
  - homelab-platform-monitors.yaml
  - homelab-prometheusrule.yaml
configMapGenerator:
  - files:
      - values.yaml=values.yaml
    name: kube-prometheus-stack-values
configurations:
  - kustomizeconfig.yaml
