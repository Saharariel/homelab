apiVersion: apps/v1
kind: Deployment
metadata:
  name: jfa-go
spec:
  template:
    spec:
      nodeSelector:
        kubernetes.io/hostname: worker-node-1
      replicas: 0 # TODO: Find / Develop a alterative for this app
